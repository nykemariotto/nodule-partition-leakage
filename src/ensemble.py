"""
STAGE 6 — soft-voting ensemble from saved probs (SPEC §3). NOT YET IMPLEMENTED.

Mean of member probabilities (config ensemble.members / method), threshold from
config ensemble.threshold. Operates only on outputs/probs/*.npz — no re-training.
"""
from __future__ import annotations


def main():
    raise NotImplementedError("STAGE 6 — implement after training.")


if __name__ == "__main__":
    main()
