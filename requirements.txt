# =====================================================================
# Pinned environment for the LIDC partitioning study (conda env `nodules`, Python 3.10).
#
# These pins are the VERIFIED versions of the environment that produced everything in
# outputs/ (read back from importlib.metadata on 2026-07-20) — not aspirational ones.
# Two divergences were found and corrected on that date (DECISIONS D27):
#   * the training stack (torch / torchvision / timm) was commented out, so
#     `pip install -r requirements.txt` produced an environment that could not train;
#   * scikit-learn was pinned to 1.7.2 while 1.3.2 is what is installed and was used.
#
# NOTE: pylidc 0.2.3 (2020) requires the legacy numpy aliases -> numpy pinned <1.24,
#       sqlalchemy pinned <2, pydicom pinned <3, setuptools <80 (provides pkg_resources).
# =====================================================================
numpy==1.23.5
scipy==1.15.3
pandas==2.3.3
pydicom==2.4.5
pylidc==0.2.3
SQLAlchemy==1.4.54
scikit-image==0.21.0
scikit-learn==1.3.2
Pillow==12.3.0
matplotlib==3.10.9
PyYAML==6.0.3
setuptools==79.0.1

# --- training stack (STAGE 4+) ---
# torch/torchvision are CUDA builds and must come from the cu121 wheel index. Install them
# FIRST with the command below, then `pip install -r requirements.txt` for everything else:
#   pip install torch==2.3.1+cu121 torchvision==0.18.1+cu121 --index-url https://download.pytorch.org/whl/cu121
torch==2.3.1+cu121
torchvision==0.18.1+cu121
timm==1.0.28

# --- optional, NOT installed and used by no result in outputs/ ---
# SimpleITK==2.5.5        # Stage-2 volume resampling
